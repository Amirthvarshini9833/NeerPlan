"use client";
import { useState } from "react";
export default function RegisterPage() { const [message, setMessage] = useState(""); return <main className="auth"><form onSubmit={async e => { e.preventDefault(); const response = await fetch("/api/auth/register", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))) }); const data = await response.json(); setMessage(response.ok ? "Account created. You can sign in now." : data.error); }}><p className="eyebrow">NEERPLAN</p><h1>Create your account</h1><label>Name<input name="name" required /></label><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" minLength={10} required /></label><button>Create account</button>{message && <p>{message}</p>}<a href="/login">Already have an account?</a></form></main>; }
