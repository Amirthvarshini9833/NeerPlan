"use client";
import { useState } from "react";
export function QuoteEntryForm({ leadId }: { leadId: string }) { const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setSaving(true); setMessage(""); const data = Object.fromEntries(new FormData(e.currentTarget)); const response = await fetch(`/api/installer/leads/${leadId}/quotes`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...data, installationDate: data.installationDate ? new Date(String(data.installationDate)).toISOString() : "" }) }); const result = await response.json(); setMessage(response.ok ? "Quote added." : result.error ?? "Unable to add quote."); if (response.ok) e.currentTarget.reset(); setSaving(false); } return <form className="quote-entry-form" onSubmit={submit}><h3>Add quote</h3><div className="fields"><label>System<select name="systemType"><option>Storage tank system</option><option>Groundwater recharge system</option><option>Hybrid storage + recharge system</option></select></label><label>Tank (L)<input name="tankLitres" type="number" min="500" /></label><label>Material<input name="material" required placeholder="HDPE, RCC, masonry" /></label><label>Warranty (months)<input name="warrantyMonths" type="number" min="0" required /></label><label>Price (₹)<input name="priceInr" type="number" min="0" required /></label><label>Installation date<input name="installationDate" type="date" /></label></div><label>Notes<textarea name="notes" maxLength={1000} /></label><button disabled={saving}>{saving ? "Saving…" : "Add quote"}</button>{message && <p className="form-message" role="status">{message}</p>}</form>; }
